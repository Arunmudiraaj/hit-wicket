1. [OPEN DECISION] If the Better Auth token validation fails in socketAuth middleware (expired/invalid token), the user silently falls back to guest mode. Current behavior: intentional — we never block the socket connection. Open question: should the client be notified (e.g. via an 'error' event) so it can show a "session expired, please log in again" UI? Or is silent guest fallback acceptable?